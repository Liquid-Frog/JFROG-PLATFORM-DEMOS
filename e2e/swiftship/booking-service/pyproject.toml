[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "swiftship-booking-service"
version = "1.0.0"
description = "SwiftShip booking service demo"
requires-python = ">=3.9"
