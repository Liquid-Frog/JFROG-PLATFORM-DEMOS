# SwiftShip Booking Service — Python / FastAPI
# VULN-SEED (CVE): starlette 0.36.3 — CVE-2024-47874 (unbounded multipart body read, DoS, CVSS 8.7) — Fix: starlette>=0.40.0
starlette==0.36.3

# fastapi pulls starlette as a dep — version constrained above
fastapi==0.109.2

# VULN-SEED (CVE): urllib3 2.2.1 — CVE-2025-66418 (chained decompression DoS, CVSS 8.9) — Fix: urllib3>=2.6.0
urllib3==2.2.1

# VULN-SEED (CVE): langflow 1.1.4 — CVE-2025-3248 (unauthenticated RCE via /api/v1/run, CVSS 9.8, CISA KEV) — Fix: langflow>=1.3.0
langflow==1.1.4

# Standard deps
requests==2.31.0
pydantic==2.5.0
uvicorn[standard]==0.27.0
python-dotenv==1.0.0
