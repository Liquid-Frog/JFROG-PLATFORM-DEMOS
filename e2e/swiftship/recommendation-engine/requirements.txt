# SwiftShip Recommendation Engine
# Serves product recommendations using a HuggingFace model via Langflow

# VULN-SEED (CVE): langflow 1.1.4 — CVE-2025-3248 (unauthenticated RCE via /api/v1/run, CVSS 9.8, CISA KEV) — Fix: langflow>=1.3.0
langflow==1.1.4

fastapi==0.109.2
uvicorn[standard]==0.27.0
pydantic==2.5.0

# HuggingFace integration
# VULN-SEED (Curation): huggingface-hub 0.21.0 pulls an unapproved community model from an unverified HF org — Fix: proxy huggingface-hub through the Artifactory HF remote repo and pre-approve model orgs via the Curation policy
transformers==4.38.0
huggingface-hub==0.21.0
torch==2.2.0

python-dotenv==1.0.0
